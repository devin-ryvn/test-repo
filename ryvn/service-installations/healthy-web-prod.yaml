kind: ServiceInstallation
metadata:
  name: healthy-web-prod
spec:
  service: healthy-web
  environment: production
  namespace: default
  config: ../configs/values.yaml
  env:
    - key: LOG_LEVEL
      value: debug
