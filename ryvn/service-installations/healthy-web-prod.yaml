kind: ServiceInstallation
metadata:
  name: healthy-web-prod
spec:
  service: healthy-web
  environment: production
  namespace: default
  config: |
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
  env:
    - key: LOG_LEVEL
      value: error
