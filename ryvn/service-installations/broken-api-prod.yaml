kind: ServiceInstallation
metadata:
  name: broken-api-prod
spec:
  service: broken-api
  environment: production
  namespace: default
  config: ../configs/values.yaml
  env:
    - key: APP_MODE
      value: api
